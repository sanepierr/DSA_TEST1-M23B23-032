# Input the length of the base and height 
base = float(input("Enter the length of the base: "))
height = float(input("Enter the height of the parallelogram: "))

# area of the parallelogram
area = base * height

# result
print(f"Area is: {area}")
